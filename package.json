{
  "name": "ai-tools-site",
  "version": "1.0.0",
  "description": "AI tools database site generator",
  "main": "build/build.js",
  "scripts": {
    "build": "node build/build.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.70.0"
  }
}
